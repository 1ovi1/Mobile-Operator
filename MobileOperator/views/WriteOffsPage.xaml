<Page x:Class="MobileOperator.views.WriteOffsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      Title="Списания"
      Background="#1A1A1A">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="История списаний средств" 
                   TextAlignment="Center" 
                   FontSize="24" 
                   Foreground="#FFD700"
                   FontWeight="Bold"
                   Margin="0,0,0,15"
                   Grid.Row="0"/>

        <!-- Выбор периода -->
        <Border Background="#2A2A2A" 
                BorderBrush="#FF8C00" 
                BorderThickness="2" 
                CornerRadius="15" 
                Padding="25" 
                Margin="0,0,0,15"
                Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Укажите период: с"
                           FontSize="18"
                           Foreground="#FFD700"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"
                           Grid.Column="0"/>
                
                <DatePicker SelectedDate="{Binding Path=From}"
                            Width="150"
                            Background="#1A1A1A"
                            Foreground="#FFA500"
                            BorderBrush="#FF8C00"
                            Margin="0,0,10,0"
                            Grid.Column="1"/>
                
                <TextBlock Text="по"
                           FontSize="18"
                           Foreground="#FFD700"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"
                           Grid.Column="2"/>
                
                <DatePicker SelectedDate="{Binding Path=Till}"
                            Width="150" Background="#1A1A1A"
                            Foreground="#FFA500"
                            BorderBrush="#FF8C00"
                            Margin="0,0,20,0"
                            Grid.Column="3"/>

                <Button Command="{Binding GetWriteOffsCommand}" 
                        Content="Сформировать выписку"
                        Foreground="#1A1A1A" Background="#FFD700" 
                        FontSize="16" FontWeight="Bold"
                        Height="40" BorderThickness="0"
                        HorizontalAlignment="Left" Grid.Column="4"/>
            </Grid>
        </Border>

        <!-- Таблица -->
        <Border Background="#2A2A2A"
            BorderBrush="#FF8C00"
            BorderThickness="2"
            CornerRadius="15"
            Padding="20"
            Margin="0,0,0,15"
            Grid.Row="2">
            <DataGrid ItemsSource="{Binding WriteOffs}"
                      FontSize="16"
                      CanUserAddRows="False"
                      AutoGenerateColumns="False"
                      ColumnWidth="*"
                      Background="#1A1A1A" Foreground="#FFA500"
                      BorderBrush="#FF8C00" BorderThickness="1"
                      RowBackground="#2A2A2A" AlternatingRowBackground="#1A1A1A"
                      HeadersVisibility="Column" GridLinesVisibility="None"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      MaxHeight="450"> <!-- ограничение высоты для включения внутреннего скролла -->
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FF8C00"/>
                        <Setter Property="Foreground" Value="#1A1A1A"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Дата" Binding="{Binding DateToString}" IsReadOnly="True" Width="1.5*"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding Category}" IsReadOnly="True" Width="1*"/>
                    <DataGridTextColumn Header="Сумма (₽)" Binding="{Binding Amount}" IsReadOnly="True" Width="0.8*"/>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Description}" IsReadOnly="True" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding PrintToExelCommand}" Content="Скачать в xlsx" Foreground="#1A1A1A" Background="#FFD700" FontSize="16" FontWeight="Bold" Height="45" Width="300" BorderThickness="0" Grid.Column="0"/>
            <Button Command="{Binding PrintToWordCommand}" Content="Скачать в doc" Foreground="#1A1A1A" Background="#FFD700" FontSize="16" FontWeight="Bold" Height="45" Width="300" BorderThickness="0" Grid.Column="1"/>
        </Grid>
    </Grid>
</Page>
